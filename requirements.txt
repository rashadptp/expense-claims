Django>=5.0,<5.2
groq>=0.11.0
Pillow>=10.0.0
python-dotenv>=1.0.0
reportlab>=4.0
anthropic>=0.1.0

# Production extras (not required for the demo)
# psycopg[binary]>=3.1     # PostgreSQL driver
# gunicorn>=21.0           # WSGI server
# celery>=5.3              # async task queue
# redis>=5.0               # broker for Celery
# boto3>=1.34              # S3 storage
